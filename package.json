{
  "name": "assistencia-tecnica",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "node backend/server.js",
    "frontend": "cd site && npx ng serve --open",
    "start": "echo 'Run in separate terminals: npm run backend & npm run frontend'"
  },
  "dependencies": {
    "@angular/cli": "^19.2.4",
    "json-server": "0.17.4"
  }
}
